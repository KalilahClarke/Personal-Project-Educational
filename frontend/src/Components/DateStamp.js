import React from 'react'


export default function DateStamp() {
    const dateTime = () =>{
        const showDate = new Date()
        const displayTodaysDate = showDate.getMonth() + '/' + showDate.getDate() + showDate.getFullYear();
    }
  return (
    <></>
  )
}
